package ling.std

class IntRange(val start: Int, val end: Int) : IndexedCollection<Int>() {
    override val size: Int get() = end - start
    override operator fun get(index: Int): Int = start + index
}
